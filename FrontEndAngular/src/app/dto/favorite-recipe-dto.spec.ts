import { FavoriteRecipeDto } from './favorite-recipe-dto';

describe('FavoriteRecipeDto', () => {
  it('should create an instance', () => {
    expect(new FavoriteRecipeDto()).toBeTruthy();
  });
});
